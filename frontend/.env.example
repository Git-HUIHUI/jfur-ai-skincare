# 晶肤AI美肤助手 - 前端环境变量配置

# 后端 API 地址（仅 Next.js 服务端代理 /api/chat 时使用，浏览器不会直接访问）
# 本地开发 / ngrok 公网分享：保持默认即可
BACKEND_URL=http://localhost:8001

# 注意：不要用 NEXT_PUBLIC_API_URL=http://localhost:8001 让浏览器直连后端，
# 否则 ngrok 公网访问时远程用户会连到自己的 localhost，导致连接失败。
